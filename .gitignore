# Python
.venv/
__pycache__/
*.pyc

# Model weights (3.4GB — download from HF mirror jetjodh/sam3)
models/

# Large media & derived artifacts (regenerated by the pipeline)
data/raw/*.mp4
data/frames/
data/masks/

# Pipeline outputs (regenerated; the viz/assets bundle is committed so the demo runs on clone)
out/

# OS
.DS_Store
